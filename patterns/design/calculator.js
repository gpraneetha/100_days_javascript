// export - makes a variable, function, class or object accessible to other modules
export function add(x, y) {
    return x+y;
} 

export function subtract(x, y) {
    return x-y;
}

export function multiply(x, y) {
    return x*y;
}

export default function square(x) {
    return x*x;
}